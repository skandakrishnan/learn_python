coordinates = (4, 5) #TupleCannot be changed Immutible
print(coordinates)
print(coordinates[0])
#diff b/w tuple and lists
#No modification on tuple but modification allowed in lists
#In a program things that are going to be unchanged stored in tuples


